    // if (!up && !low && !num && !sym)

    // if (!up && !low && !num && sym)

    // if (!up && !low && num && !sym)

    // if (!up && low && !num && !sym)

    // if (up && !low && !num && !sym)

    // if (!up && !low && num && sym)

    // if (!up && low && num && !sym)

    // if (!up && !low && !num && !sym)

    // if (!up && low && !num && sym)

    // if (up && low && !num && !sym)

    // if (up && !low && !num && sym)

    // if (up && !low && num && !sym)

    // if (!up && low && !num && sym)

    // if (!up && low && num && sym)

    // if (up && !low && num && sym)

    // if (up && low && !num && sym)

    // if (up && low && num && !sym)

    // if (up && low && num && sym)